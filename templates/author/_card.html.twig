<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Author Card</title>
    <style>
        .author-card {
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #fff;
            margin: 20px;
            padding: 20px;
            width: 300px;
            text-align: center;
            overflow: hidden;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .author-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .author-card h3 {
            font-size: 1.25rem;
            margin: 10px 0;
        }

        .author-card span {
            display: block;
            margin: 5px 0;
            font-size: 1rem;
            color: #333;
        }

        .author-card .actions {
            margin-top: 15px;
        }

        .author-card button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.9rem;
            margin: 0 5px;
            transition: background-color 0.2s;
        }

        .author-card button:hover {
            background-color: #0056b3;
        }

        .author-card form {
            display: inline;
        }

        .author-card form button {
            background-color: #dc3545;
        }

        .author-card form button:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>
<div class="author-card">
    <!-- Affichage des informations de l'auteur -->
    <h3>{{ author.lastName }}</h3>
    <h3>{{ author.firstName }}</h3>
    <span>Date de naissance : {{ author.birthDate|date('d/m/Y') }}</span>

    <!-- Affiche les boutons de modification uniquement pour l'auteur lui-même ou les utilisateurs ayant écrit des livres de cet auteur -->
    {% if app.user and (app.user == author or app.user in author.books|map(book => book.user)|filter(user => user == app.user)) %}
        <div class="actions">
            <a href="{{ path('app_author_update', { id: author.id }) }}">
                <button>Modifier</button>
            </a>
            <form action="{{ path('app_author_remove', { id: author.id }) }}" method="post">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.id) }}">
                <button type="submit">Supprimer</button>
            </form>
        </div>
    {% endif %}
</div>
</body>
</html>
