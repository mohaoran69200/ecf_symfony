<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Card</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }

        .book-card {
            background: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin: 20px auto;
            max-width: 300px;
            padding: 20px;
            text-align: center;
        }

        .book-card h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
            color: #333;
        }

        .book-card span {
            display: block;
            margin-bottom: 10px;
            color: #666;
        }

        .book-card a {
            color: #007bff;
            text-decoration: none;
        }

        .book-card a:hover {
            text-decoration: underline;
        }

        .actions {
            margin-top: 20px;
        }

        .actions button {
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            color: #fff;
            cursor: pointer;
            font-size: 1em;
            padding: 10px 20px;
            margin-right: 10px;
        }

        .actions button:hover {
            background-color: #0056b3;
        }

        .actions form {
            display: inline;
        }

        .actions input[type="hidden"] {
            display: none;
        }

        .actions button.delete {
            background-color: #dc3545;
        }

        .actions button.delete:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>
<div class="book-card">
    <a href="{{ path('app_book_show', { id: book.id }) }}">
        <h3>{{ book.title }}</h3>
        <span>{{ book.description }}</span>

        <!-- Affichage de l'auteur -->
        <span>
            <a href="{{ path('app_author_show', { id: book.author.id }) }}">
                {{ book.author.firstName }} {{ book.author.lastName }}
            </a>
        </span>

        <!-- Affichage de l'utilisateur -->
        <span>
            <a href="{{ path('app_user_show', { id: book.user.id }) }}">
                    {{ book.user.email }}
            </a>
        </span>

        {% if app.user and app.user == book.user %}
            <div class="actions">
                <a href="{{ path('app_book_update', { id: book.id }) }}">
                    <button>Modifier</button>
                </a>

                <form action="{{ path('app_book_remove', { id: book.id }) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.id) }}">
                    <button type="submit" class="delete">Supprimer</button>
                </form>
            </div>
        {% endif %}
    </a>
</div>
</body>
</html>
